{
  "name": "zeldalike2",
  "version": "1.0.0",
  "description": "npm install -g http-server nodemon http-server . -c-1 -o",
  "main": "index.js",
  "dependencies": {
    "nodemon": "^2.0.4",
    "npm-run-all": "^4.1.5",
    "phaser": "^3.24.1"
  },
  "devDependencies": {},
  "scripts": {
    "hofclone": "tsc --watch -p tsconfig.json",
    "http-server": "http-server bin/ -c-1 -o",
    "start": "npm-run-all --parallel hofclone http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
